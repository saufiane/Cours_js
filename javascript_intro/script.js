// nombres, chaînes de caractères et booléens

let nombre = 50;

let texte = "salut les amis";

texte = `bonjour, j'ai ${nombre + 50} ans.`;
let autretexte = "bonjour, j'ai " + nombre + 50 + " ans .";

let boolean = true;

console.log(texte, autretexte);